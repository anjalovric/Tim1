<Window x:Class="InitialProject.View.TourForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:InitialProject.View"
        mc:Ignorable="d"
        Title="TourForm" Height="450" Width="800">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Label Grid.Row="0" Grid.Column="0">Name:</Label>
            <Label Grid.Row="1" Grid.Column="0">Maximum number of guests</Label>
            <Label Grid.Row="2" Grid.Column="0">Duration of tour in hours</Label>
            <Label Grid.Row="3" Grid.Column="0">If tour has started check point</Label>

            <TextBox x:Name="TourName" Grid.Row="0" Grid.Column="1" Text="{Binding Path=Tour.Name,Mode=OneWayToSource,UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <TextBox x:Name="MaxGuests" Grid.Row="1" Grid.Column="1" Text="{Binding Path=Tour.MaxGuests,Mode=OneWayToSource,UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <TextBox x:Name="Duration" Grid.Row="2" Grid.Column="1" Text="{Binding Path=Tour.Duration,Mode=OneWayToSource,UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <CheckBox x:Name="StartedTour" Grid.Row="3" Grid.Column="1"></CheckBox>
            <Button Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Click="Button_Click">Potvrdi</Button>
        </Grid>
        
    </Grid>
</Window>
